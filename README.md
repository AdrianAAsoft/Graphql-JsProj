# Graphql-JsProj
A base project for understanding GraphQL in a JavaScript environment

You need dockerHub installed because the database runs on docker

---
Comand bash for dependencies and then testing - In root Folder
```
---
docker-compose up -d --build
```

---

Comands for taking down the docker with the data - In root Folder
```
docker-compose down -v
```
